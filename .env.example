# Copy to .env and fill in real values. .env is gitignored — never commit it.

# Backend JWT signing secret (use a long random string, e.g. `openssl rand -hex 32`)
JWT_SECRET=change-me

# PostgreSQL password for the app DB user
DB_PASSWORD=change-me
